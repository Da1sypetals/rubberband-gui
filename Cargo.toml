[package]
name = "rubberband-gui"
version = "0.1.0"
edition = "2024"

[dependencies]
iced = { path = "../iced", features = ["tokio"] }
tokio = { version = "1.0", features = ["process"] }
rfd = "0.16"
